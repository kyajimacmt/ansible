- name: 'OS Set up'
  hosts: [ windows ]
  gather_facts: no
  tags: OS

  tasks:
    - name: Add a user
      win_user:
        name: 'ansible'
        password: 'p@ssw0rd'
        groups: 'Users'

    - name: 'Create directory structure'
      win_file:
        path: 'C:\CMT\log\server'
        state: 'directory'

    - name: 'Create directory structure'
      win_file:
        path: 'C:\CMT\log\storage'
        state: 'directory'
